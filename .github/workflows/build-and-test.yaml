name: build-and-test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: dotnet build
        with:
          path: ./server
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: dotnet test
